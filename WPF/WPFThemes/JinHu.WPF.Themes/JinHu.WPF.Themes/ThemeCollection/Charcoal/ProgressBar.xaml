<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:s="clr-namespace:System;assembly=mscorlib">
  <ResourceDictionary.MergedDictionaries>
    <ResourceDictionary Source="Shared.xaml"/>
  </ResourceDictionary.MergedDictionaries>

  <!--<Style x:Key="{x:Type ProgressBar}"
       TargetType="{x:Type ProgressBar}">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type ProgressBar}">
          <Grid MinHeight="14"
              MinWidth="200"
              Background="{TemplateBinding Background}">
            <VisualStateManager.VisualStateGroups>
              <VisualStateGroup x:Name="CommonStates">
                <VisualState x:Name="Determinate" />
                <VisualState x:Name="Indeterminate">
                  <Storyboard>
                    <ObjectAnimationUsingKeyFrames Duration="00:00:00"
                                                 Storyboard.TargetName="PART_Indicator"
                                                 Storyboard.TargetProperty="Background">
                      <DiscreteObjectKeyFrame KeyTime="00:00:00">
                        <DiscreteObjectKeyFrame.Value>
                          <SolidColorBrush>Transparent</SolidColorBrush>
                        </DiscreteObjectKeyFrame.Value>
                      </DiscreteObjectKeyFrame>
                    </ObjectAnimationUsingKeyFrames>

                  </Storyboard>
                </VisualState>
              </VisualStateGroup>
            </VisualStateManager.VisualStateGroups>
            <Border x:Name="PART_Track" CornerRadius="2" BorderThickness="1"
                    BorderBrush="{StaticResource Brush_Border_Normal}"/>
            <Border x:Name="PART_Indicator" CornerRadius="2" BorderThickness="1"
                    HorizontalAlignment="Left" Margin="0,-1,0,1"
                    Background="{TemplateBinding Foreground}"
                    BorderBrush="{StaticResource Brush_Border_Pressed}">
              <Grid ClipToBounds="True"
                  x:Name="Animation">
                <Rectangle x:Name="PART_GlowRect"
                         Width="100"
                         HorizontalAlignment="Left"
                         Fill="{StaticResource Brush_Background_Checked}"
                         Margin="-100,0,0,0" />
              </Grid>
            </Border>
          </Grid>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
    <Setter Property="Background" Value="{StaticResource Brush_Background_Normal}">
    
    </Setter>
    <Setter Property="Foreground" Value="{StaticResource Brush_Background_MouseOver}">
     
    </Setter>
  </Style>-->

  <ControlTemplate x:Key="ProgressBarControlTemplate" TargetType="ProgressBar">
    <Grid Name="TemplateRoot" SnapsToDevicePixels="True">
      <VisualStateManager.VisualStateGroups>
        <VisualStateGroup x:Name="CommonStates">
          <VisualState x:Name="Determinate"/>
          <VisualState x:Name="Indeterminate"/>
        </VisualStateGroup>
        <VisualStateGroup x:Name="ValidationStates">
          <VisualState x:Name="Valid"/>
          <VisualState x:Name="InvalidFocused"/>
          <VisualState x:Name="InvalidUnfocused"/>
        </VisualStateGroup>
      </VisualStateManager.VisualStateGroups>
      <Border CornerRadius="2" Background="{DynamicResource Brush_Background_MouseOver}" BorderThickness="2"/>
      <Border Name="PART_Track" Margin="1" CornerRadius="2" Background="{DynamicResource Brush_Background_Normal_Track}"/>
      <Border Name="PART_Indicator" Margin="1" HorizontalAlignment="Left">
          <Grid Name="Animation" ClipToBounds="True" Background="{DynamicResource Brush_Background_MouseOver}">
            <Rectangle Name="PART_GlowRect" 
                       Width="50" 
                       Margin="-100,0,0,0" 
                       HorizontalAlignment="Left"
                       RadiusX="5" RadiusY="5"
                       Fill="{DynamicResource Brush_Foreground_MouseOver}"/>
          </Grid>
      </Border>
    </Grid>
    <ControlTemplate.Triggers>
      <!--<Trigger Property="Orientation">
        <Setter Property="LayoutTransform" TargetName="TemplateRoot">
          <Setter.Value>
            <RotateTransform Angle="-90"/>
          </Setter.Value>
        </Setter>
        <Trigger.Value>
          <x:Static Member="Orientation.Vertical"/>
        </Trigger.Value>
      </Trigger>-->
      
      <!--<Trigger Property="IsIndeterminate" Value="True">
        <Setter Property="UIElement.Visibility" Value="Collapsed" TargetName="Indicator"/>
        <Setter Property="Fill" Value="{StaticResource Brush_Background_MouseOver}" TargetName="Indicator"/>
      </Trigger>-->

      <!--<Trigger Property="IsIndeterminate" Value="False">
        <Setter Property="Background" Value="{StaticResource Brush_Background_MouseOver}" TargetName="Animation"/>
      </Trigger>-->
    </ControlTemplate.Triggers>
  </ControlTemplate>

  <Style TargetType="{x:Type ProgressBar}">
    <Setter Property="Foreground" Value="{DynamicResource Brush_Foreground_MouseOver}"/>
    <Setter Property="Template" Value="{StaticResource ProgressBarControlTemplate}"/>
  </Style>

</ResourceDictionary>